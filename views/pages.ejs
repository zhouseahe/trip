<nav>
  <ul class="pagination">
    <% for(var i =0; i< pages.length;i++){ %>
      <li  <%if(page==pages[i]){%> class="disabled" <%}%> ><a href="<%=url+pages[i] %>"><%=pages[i]%></a></li>
    <% } %>
  </ul>
</nav>